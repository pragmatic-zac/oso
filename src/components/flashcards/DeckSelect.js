import React, { Component } from "react";
import "./DeckSelect.css";

// this is where the user will select their deck to be quizzed on

export default class DeckSelect extends Component { 
    render () {
        return (
            "hey there, go ahead and select your deck"
        )
    }
}